# 코스 등록용 ZIP 파일 포맷

golf-tracker의 "새 코스 추가" 모달에서 ZIP 파일 하나로 골프장의 야디지/그린 이미지와 홀 정보(파, 공략)를 한 번에 등록할 수 있습니다. 이 문서는 그 ZIP이 따라야 하는 구조를 설명합니다.

파서 소스: `app/utils/zipCourseImport.js`

## 전체 구조

```
(골프장 폴더 — 있어도 없어도 됨)/
  코스A이름/
    야디지 이미지 9장 (jpg/png/webp)
    그린 이미지 9장 또는 0장
  코스B이름/
    (동일)
  코스정보.json  ← 파일명은 자유, 확장자만 .json이면 됨
```

- 골프장 이름의 폴더로 한 겹 더 감싸도 되고, 안 감싸도 됩니다(예: `써닝포인트CC/SUN/...` 이든 `SUN/...` 이든 둘 다 인식).
- 9홀짜리 서브코스가 몇 개든 상관없습니다(전반/후반 2개, 벨리/와일드/에코 3개, EAST/WEST/SOUTH 3개 등).
- **9홀을 2번 도는 골프장**(서브코스가 1개뿐)도 지원됩니다 — 등록 시 "9홀을 2번 도는 골프장입니다" 체크박스를 켜면 같은 코스를 복제해 OUT/IN 2개로 만듭니다.

## JSON (`코스정보.json`)

```json
{
  "골프장": "골프장 이름",
  "거리단위": "m",
  "SUN코스": {
    "코스명": "SUN",
    "1홀": { "파": 4, "공략": "홀 공략 텍스트" },
    "2홀": { "파": 3, "공략": "..." },
    "...": "9홀까지",
    "9홀": { "파": 5, "공략": "..." }
  },
  "POINT코스": {
    "코스명": "POINT",
    "1홀": { "파": 4, "공략": "..." },
    "...": "9홀까지"
  }
}
```

규칙:

- `골프장` 키(문자열)만 제외하면, **최상위(또는 `코스` 키 아래)의 나머지 모든 객체 키는 서브코스 하나**로 취급됩니다. 키 이름은 자유입니다 — `전반코스`/`후반코스`, `SUN코스`/`POINT코스`, `EAST코스`/`WEST코스` 등 무엇이든 가능하고, 개수 제한도 없습니다(2개, 3개, 4개+ 모두 지원).
- 실제 골프장 공식 데이터처럼 최상위가 아니라 `"코스": { "EAST코스": {...}, "WEST코스": {...} }` 형태로 한 겹 더 감싸져 있어도 인식합니다.
- 각 서브코스 객체 안의 `코스명` 필드는 선택 사항입니다. 없으면 객체의 키 이름(`SUN코스` 등)을 코스명으로 사용합니다.
- 홀 정보는 `"1홀"`부터 `"9홀"`까지 9개가 있어야 하며, 각 홀 객체에서 실제로 쓰이는 필드는 `파`(숫자)와 `공략`(문자열, 없으면 빈 문자열)뿐입니다. 홀이름/핸디캡/거리(블루·화이트 등)/GPS기준점 같은 다른 필드가 더 있어도 무시하고 무해합니다 — 즉 골프장 공식 데이터를 그대로 넣어도 됩니다.
- 파는 3~6 등 어떤 숫자든 가능합니다(파6 홀 지원).

## 이미지 폴더

- 각 서브코스는 자기 이름과 일치(또는 유사)하는 폴더를 찾아 그 안의 이미지를 사용합니다.
  - 폴더명 매칭은 완전 일치 → 부분 포함 → 공백/대소문자/"코스"·"course" 접미사 무시 후 비교, 순으로 시도합니다. 예: JSON 키 `EAST코스`와 실제 폴더명 `EAST Course`도 매칭됩니다.
  - 이름 매칭에 실패하면, 아직 안 쓴 폴더 중 야디지 이미지가 정확히 9장인 폴더를 순서대로 찾아 폴백으로 사용합니다.
- 폴더 하위 구조는 자유입니다(`코스야디지`/`코스이미지`/`yardage`/`야디지` 등 폴더명 무관). 파서는 폴더 아래 모든 이미지를 훑어서:
  - 경로나 파일명에 **"그린" 또는 "green"**이 들어가면 **그린 이미지**로,
  - **"언듈레이션" 또는 "undulation"**이 들어가면 **무시**(그린 굴곡도 참고용 이미지 등 — 코스 등록에 쓰지 않음),
  - 그 외 나머지 이미지는 전부 **야디지 이미지**로 분류합니다.
- 이미지 확장자는 `.jpg`, `.jpeg`, `.png`, `.webp`를 인식합니다.
- 파일명에 포함된 숫자를 기준으로 1~9홀 순서로 정렬합니다(`1.jpg`, `2.jpg`, ... 또는 `SUN_H01.png` 처럼 숫자가 들어간 형태면 자동 정렬).

## 개수 검증

- 서브코스마다 **야디지 이미지는 정확히 9장**이어야 합니다. 아니면 `'{폴더명}' 폴더의 야디지(코스) 이미지가 9장이 아닙니다(N장)` 오류가 발생합니다.
  - 자주 발생하는 원인: 그린/언듈레이션이 아닌 이름으로 된 부가 이미지 폴더가 섞여서 개수가 초과되는 경우. 이런 폴더가 있다면 이름에 위 키워드가 없으면 야디지로 오분류되니, 폴더 구조를 정리하거나 파서의 `IGNORE_HINT`/`GREEN_HINT` 정규식에 키워드를 추가해야 합니다.
- **그린 이미지는 선택 사항**이며, 있다면 9장이어야 합니다(0장이면 앱 기본 그린 이미지를 사용). 0장도 9장도 아니면 오류가 발생합니다.

## 최소 예시

```
써닝포인트CC/
  SUN/
    야디지/
      SUN_H01.png ... SUN_H09.png
    그린/
      SUN_H01.jpg ... SUN_H09.jpg
  POINT/
    야디지/
      POINT_H01.png ... POINT_H09.png
    그린/
      POINT_H01.jpg ... POINT_H09.jpg
  코스정보.json
```

이 구조를 ZIP으로 압축해서 업로드하면, 골프장 이름(`골프장` 필드)을 코스 ID로, `SUN`/`POINT`를 서브코스(OUT/IN 등)로 등록합니다.
